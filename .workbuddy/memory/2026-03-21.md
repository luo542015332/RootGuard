## RootGuard CI/CD 修复记录

### 2026-03-21 修复内容

1. **ic_notification.xml 颜色属性问题**
   - 将 `?attr/colorControlNormal` 替换为具体颜色 `#FFFFFF`
   - 文件: `app/src/main/res/drawable/ic_notification.xml`

2. **ViewModel 中 UseCase 命名冲突**
   - `clearLogs()` → `onClearLogs()`
   - `toggleModule()` → `onToggleModule()`
   - `uninstallModule()` → `onUninstallModule()`
   - 避免方法名与注入的 UseCase 变量名冲突

3. **LogLevel 类型转换问题**
   - 在 MagiskRepository 中添加 LogLevel 类型转换
   - 将 data.magisk.LogLevel 转换为 ui.screens.logs.LogLevel

4. **ExperimentalMaterial3Api 警告**
   - 在 HomeScreen.kt 和 ThemeSettingsScreen.kt 中添加 `@OptIn(ExperimentalMaterial3Api::class)`

5. **GitHub Actions 权限问题**
   - 在 build.yml 中添加 `permissions: contents: write`
   - 允许 GitHub Actions 创建 Release

6. **ModuleBackupManager 变量名冲突**
   - 修复 `deleteBackup()` 中的变量名冲突
   - 将局部变量 `backupDir` 重命名为 `backupDirToDelete`

7. **日志提取闪退问题**
   - 改进 LogsViewModel 错误处理，避免空状态崩溃
   - 改进 MagiskProvider.getLogs()，使用 logcat 替代不稳定的 magisk -l
   - 改进 parseLogLine() 添加异常保护
   - 限制日志数量为 100 条避免内存问题

8. **应用图标配置更新**
   - 更新所有 mipmap 文件夹的图标配置以使用 PNG 文件
   - 添加图标自动更新脚本 (update-icon.ps1)
   - 添加图标文件夹准备脚本 (prepare-icon-folders.bat)
   - 添加详细的图标更新指南文档
   - 准备好接收用户提供的熊猫图标

9. **GitHub Actions 图标资源缺失修复**
   - 修复错误: `resource mipmap/ic_launcher not found`
   - 问题原因: XML 配置文件只有注释，没有实际引用资源
   - 解决方案: 使用 Android 默认图标作为临时方案，所有分辨率引用 `@android:drawable/ic_menu_gallery`
   - 修改文件: 12 个图标配置文件 (mipmap-*/ic_launcher*.xml)
   - GitHub Actions 现在应该可以正常构建

10. **应用图标更新为可爱熊猫**
    - 从 Flaticon 下载免费可商用的熊猫图标
    - 自动生成所有所需分辨率（36x36 到 144x144）
    - 替换所有 mipmap 文件夹的 PNG 图标
    - 删除旧的 XML 配置文件，使用原生 PNG
    - 已提交并推送到 GitHub
    - 图标源文件: panda.png (28567 字节)

11. **版本号同步修复**
    - 修复问题: APK 内部版本号与文件名版本号不一致
    - 更新 app/build.gradle: versionCode 2 → 3, versionName "1.1.0" → "1.2.0"
    - 更新 .github/workflows/build.yml: 使用固定版本号 v1.2.0
    - 同步 APK 文件名、Release 标签和内部版本号
    - 已提交并推送到 GitHub

12. **启动闪退问题修复** (2026-03-21 23:23)
    - 问题原因: 项目目录结构混乱，主项目 app/ 目录为空，GitHub Actions 构建的是 RootGuard/ 目录的旧代码
    - 解决方案: 将 RootGuard/app/src/main 的所有源代码同步到主项目 app/src/main
    - 提交内容: 68 个文件，10354 行代码
    - 提交信息: "fix: 修复项目结构，同步源代码到主目录，解决启动闪退问题"

13. **v1.3.0 版本发布** (2026-03-21 23:35)
    - 卸载旧版本 APK
    - 更新版本号: v1.2.0 → v1.3.0
    - 更新 versionCode: 3 → 4
    - 更新 GitHub Actions 构建配置（APK 文件名、Release 配置）
    - 提交信息: "release: 发布 PandaSU v1.3.0 - 修复启动闪退问题"
    - GitHub Actions 正在构建 v1.3.0 并创建 Release

14. **图标重复资源错误修复** (2026-03-21 23:39)
    - 问题原因: 每个分辨率的图标既有 .png 文件又有 .xml 文件，导致资源冲突
    - 解决方案: 删除所有 10 个 PNG 图标文件（ic_launcher.png 和 ic_launcher_round.png）
    - 保留: 矢量图标的 XML 文件
    - 提交信息: "fix: 删除重复的 PNG 图标文件，修复构建错误"
    - GitHub Actions 正在重新构建 v1.3.0

### 构建状态
- 所有 Kotlin 编译错误已修复
- CI 构建成功
- APK 可以正常生成
- 图标已更新为可爱的熊猫图标
- 版本号已更新为 v1.3.0
- GitHub Actions 正在构建 v1.3.0 Release
- GitHub Actions 图标资源问题已修复
- 启动闪退问题已修复，源代码已同步到主目录
- 等待 v1.3.0 构建完成后安装测试
